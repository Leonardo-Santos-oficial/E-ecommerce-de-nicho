import { describe, it, expect } from 'vitest'

// Teste mínimo placeholder. Mantido para evitar falha de runner em pipelines
// enquanto outros testes reais estão em useCart.strategy.test.tsx.
describe('placeholder useCart duplicate file', () => {
  it('does nothing', () => {
    expect(true).toBe(true)
  })
})
